

# Experimental code


# using ... in functions

#t1 <- function(df, ...) {
#  object <- list(...)
#  return(object)
#}


#x <- sym("accident_year")
#y <- sym("location_easting_osgr")
#r <- sym("iso_year")
#s <- sym("month_year")


#t1 <- function(df, ...) {
#  object <- as.list(substitute(list(...)))[-1L]
#  return(object)
#}

#t1 <- function(df, ...) {
#  object <- list(...)
#  return(object)
#}
